/*
// Remove duplicate element from sorted Linked List
Problem Statement:
Given a singly linked list in sorted order, your task is to remove duplicates from the linked list, such that each element appears only once.

Constraints:
The length of the linked list is at most 1000.
Input Format:
The head of a sorted singly linked list.
Output Format:
the linked list after removing duplicates.

Examples:
Input : 
Sorted Linked List: 1 -> 1 -> 2 -> 3 -> 3 -> 3 -> 4 -> 5 -> 5
Output :
Linked List: 1 -> 2 -> 3 -> 4 -> 5
*/

import {ListNode} from './00-ListNode.js'

var deleteDuplicates = function (head) {
   
};